
int *range(int start, int end)
{
    int *tab;

    tab = malloc(sizeof(int) * (end - start));
    while (stat < end)
    {
        tab[i] = strat;
        start++;
        i++;
    }
    while (start > end)
    {
        tab[i] =start;
        start--;
        i++;
    }


}
int *rrange(int start, int end)
{
    int *tab;

    tab = malloc(sizeof(int) * (end - start));
    while (stat < end)
    {
        tab[i] = end;
        end--;
        i++;
    }
    tab [i]= end;
    while (start > end)
    {
        tab[i] =end;
        end++;
        i++;
    }
    tab [i]= end;



}